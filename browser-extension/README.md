# StudyFlow Browser Class Logging

1. Open `brave://extensions`.
2. Enable Developer mode.
3. Choose **Load unpacked** and select this `browser-extension` folder.
4. Open the extension options page and paste the token from StudyFlow Settings.
5. Add approved class URL patterns inside StudyFlow.
